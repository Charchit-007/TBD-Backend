PORT=5000
NODE_ENV=development

# Local Docker Compose Postgres (see docker-compose.yml in this repo)
DATABASE_URL=postgres://postgres:postgres@postgres:5432/appdb

# In production (Render), set this to the "Internal Database URL"
# from your Render Postgres instance dashboard instead.